[project]
name = "actuarial-genai-rag"
version = "0.1.0"
description = "RAG system applied to the actuarial sector"
requires-python = ">=3.11"
dependencies = [
    # LLM & embeddings
    "anthropic>=0.40.0",
    "openai>=1.0.0",
    "sentence-transformers>=3.0.0",
    # Vector store & RAG
    "chromadb>=0.5.0",
    "langchain>=0.3.0",
    "langchain-community>=0.3.0",
    # Document processing
    "pypdf>=4.0.0",
    "python-docx>=1.1.0",
    "openpyxl>=3.1.0",
    # Data & EDA
    "pandas>=2.2.0",
    "numpy>=1.26.0",
    "matplotlib>=3.9.0",
    "seaborn>=0.13.0",
    # Notebooks
    "jupyter>=1.1.0",
    "ipykernel>=6.29.0",
    # Utilities
    "python-dotenv>=1.0.0",
    "pydantic>=2.7.0",
    "pyyaml>=6.0.0",
    "rich>=13.0.0",
    "tqdm>=4.66.0",
    "plotly>=6.7.0",
    "pyarrow>=24.0.0",
    "scikit-learn>=1.8.0",
    "spacy>=3.8.14",
    "bertopic>=0.17.4",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "pytest-cov>=5.0.0",
    "ruff>=0.4.0",
    "mypy>=1.10.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
    "pytest-cov>=5.0.0",
    "ruff>=0.4.0",
    "mypy>=1.10.0",
    "types-pyyaml>=6.0.0",
    "pre-commit>=3.7.0",
    "vulture>=2.14",
    "bandit>=1.8.0",
    "detect-secrets>=1.5.0",
]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]

[tool.ruff.lint.per-file-ignores]
"*.ipynb" = ["E501"]

[tool.mypy]
python_version = "3.11"
strict = false
ignore_missing_imports = true

[tool.bandit]
exclude_dirs = ["tests"]
skips = ["B101"]  # allow assert statements

[tool.hatch.build.targets.wheel]
packages = ["src/actuarial_genai_rag"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
